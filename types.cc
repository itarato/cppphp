#ifndef CC_TYPES
#define CC_TYPES

#include <utility>
#include "token.cc"

using namespace std;

typedef char char_type;
typedef pair<char_type, bool> char_result_type;
typedef pair<char, char> char_range_type;

#endif